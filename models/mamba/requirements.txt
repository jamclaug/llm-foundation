# Mamba experiments dependencies
# Install with: pip install -r requirements.txt

# Core dependencies (inherited from main repo)
torch>=2.0.0
numpy>=1.24.0
transformers>=4.40.0  # For pretrained Mamba models from HuggingFace
datasets>=2.12.0
tokenizers>=0.13.0

# Training utilities
tqdm>=4.65.0
# wandb>=0.15.0  # Optional: for experiment tracking

# Development
pytest>=7.3.0

# =============================================================================
# OPTIMIZED MAMBA (Linux/WSL only - requires CUDA)
# =============================================================================
# These packages provide CUDA-optimized selective scan kernels for 10-50x speedup.
# Install in order: causal-conv1d first, then mamba-ssm.
#
# pip install causal-conv1d>=1.4.0
# pip install mamba-ssm>=2.0.0
#
# If pip install fails, install from source:
#   pip install packaging ninja
#   pip install causal-conv1d --no-build-isolation
#   pip install mamba-ssm --no-build-isolation
#
# Requirements:
#   - Linux or WSL (Windows not supported natively)
#   - CUDA 11.6+ with matching PyTorch
#   - GCC/G++ for compilation
# =============================================================================
causal-conv1d>=1.4.0  # Optimized causal conv1d for Mamba
mamba-ssm>=2.0.0      # Official Mamba implementation with CUDA kernels

# Optional: For Mamba2 with even faster kernels
# triton>=2.1.0  # Used by Mamba2's SSD algorithm
